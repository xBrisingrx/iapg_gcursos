<%= form_with(model: company_category, builder: BootstrapBuilder) do |form| %>
  <div class="form-group row mb-2">
    <%= form.label :name, "Nombre"%>
    <%= form.text_field :name, required: true %>
    <%= inline_error_for(:name, company_category) %>
  </div>

  <div class="form-group row mb-2">
    <%= form.label :description, "Descripción"%>
    <%= form.text_field :description %>
    <%= inline_error_for(:description, company_category) %>
  </div>

  <div class="form-group row mb-2">
    <%= form.label :quota, "Cupos que puede reservar"%>
    <%= form.number_field :quota, required: true %>
    <%= inline_error_for(:quota, company_category) %>
  </div>

  <div class='mt-2'>
    <%= form.submit 'Guardar' %>
    <button type="button" class="btn btn-sm btn-danger" data-bs-dismiss="modal">Cancelar</button>
  </div>
<% end %>
